node_modules
dist
.git
.github
.env
# Não vão para a imagem: o build só precisa de src/, prisma/, views/, public/,
# config/ e marketing/. (As entradas *.db daqui eram resíduo da época em que o
# banco era SQLite — o projeto usa PostgreSQL desde então.)
test
*.log
